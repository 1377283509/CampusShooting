<view class="card">
	<view class="card-image" bindtap="navToDetail" data-url="{{skill.url}}">
		<image mode="aspectFill" src="{{ skill.cover }}" style="width:100%;height:100%"></image>
		<view class="image-text">
			<view>{{ skill.title }}</view>
		</view>
	</view>
</view>